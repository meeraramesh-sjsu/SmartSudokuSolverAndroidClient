<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sudoku.edu.sjsu.sudokusover">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppBaseTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".NewUser" />
        <activity
            android:name=".Tokens"
            android:parentActivityName=".NewUser" />
        <activity
            android:name=".LoginUser"
            android:parentActivityName=".NewUser" />
        <activity
            android:name=".ClientRegistration"
            android:label="@string/title_activity_client_registration" />
        <activity
            android:name=".BootStrap"
            android:label="@string/title_activity_boot_strap" />
        <activity android:name=".sudokuResult"></activity>
    </application>

</manifest>